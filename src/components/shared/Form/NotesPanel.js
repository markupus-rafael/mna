import React from "react";
import Paper from "../Paper/Paper";

const NotesPanel = ({displayError, ...props}) => {
    return (
        <Paper>
            
        </Paper>
    );
};

export default NotesPanel;
